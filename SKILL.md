---
name: community-research
description: 协作式社区洞察调研技能（Community Insight Research）。用于从公开社区讨论中了解产品、模型、公司或话题的真实体验、评价、风险、争议、用户需求与替代方案，并产出可回查的研究结论。先由用户确定调研目的与重点，AI 再做小范围试搜；用户可根据初步发现决定深挖方向、范围和报告形式。仅在调研问题可独立拆分且用户选择常规/深度调研时，按需使用子 Agent。支持平台由用户选择（默认小红书+知乎），可选执行底座为 OpenCLI；实际可用平台以运行时探测为准。
    触发词：调研 / 口碑 / 评价 / 避坑 / 值不值得 / 大家怎么看 / 有什么黑料 / 负面消息 / 用户反馈。
    用户身份预设：无特定职业或技术背景预设；默认采用中性、清晰、可回查的表达。所有输出必须中文、结构清晰、含来源标注、含限制声明。默认协作模式，不在未确认研究问题时全量抓取。
---

# 社区洞察调研（Community Insight Research）

> **副标题：从社区真实声音到可回查的研究结论**

把“某某东西到底怎么样”变成一份**由用户掌握研究方向、AI 负责搜集验证、有证据可回查**的可信报告。

> **协作原则**：AI 不应猜测用户为什么调研。先用简短选择确认研究问题，再试搜；首轮发现出来后，用户决定是否深入、转向或直接收束。只有可清晰拆分且确有收益的工作，才启用子 Agent。

> **负责任使用与合规边界**：本 Skill 仅用于用户有权访问的公开或已获授权内容。不得绕过登录、验证码、访问频率限制、反爬机制或其他平台安全措施；不得索取、保存、输出或提交账号密码、Cookie、Token 等凭据；不得将社区内容、个人信息或抓取结果未经审查地再分发。使用者应自行确认符合目标平台规则、适用法律及所在组织的数据政策。

## 适用场景

- 用户问："XX 值不值得买 / 订阅 / 用？"
- 用户问："大家对 XX 的评价如何 / 口碑怎么样？"
- 用户问："XX 有什么黑料 / 避坑 / 负面消息？"
- 用户问："XX 和 YY 哪个好？"（选型对比）
- 用户给出一个话题/品牌/模型，想要"社区真实声音"而不是官方宣传

## 不适用场景

- 需要**实时行情/硬数据**（股票、价格、天气）→ 用对应数据工具
- 只查**单一事实**（某个产品官网功能、某公司地址）→ 直接查官网
- 用户明确只要**个人观点**，不需要样本支撑 → 不需要走全流程

---

## 协作输入与控制方式

### 必须先确认的最小信息

除非用户已经在请求中清楚给出，**不得直接启动全量搜索**。先用 `mcp__ask-user__ask_user` 提供简短选择卡，最多询问下列缺失项；用户可随时说“你先按默认建议做”。

| 需要确认的事 | 选项/默认建议 | 它决定什么 |
|---|---|---|
| 调研目的 | 购买/选型决策；产品/竞品洞察；避坑/风险核查；市场舆情；内容选题 | 搜索词、筛选标准与结论结构 |
| 最在意的维度 | 真实体验；负面/风险；价格价值；功能能力；服务/售后；替代方案 | 首轮试搜的优先方向 |
| 目标人群 | 普通消费者；专业使用者；组织/企业用户；用户指定人群；不限 | 样本筛选和关键词 |
| 调研深度 | 快速摸底；常规调研；深度调研 | 样本目标、是否启用子 Agent |
| 协作模式 | 快速模式；**协作模式（默认）**；研究模式 | 用户在中间参与决策的频率 |
| 输出视角/内容形式 | 中性通用（默认）；决策行动；产品/业务洞察；风险避坑；证据优先；用户自定义 | 语言、信息排序、建议深度与最终呈现方式 |

**卡片使用规则**：
- 用户已明确上述内容时，直接复述本轮“研究简报”供其纠正，不重复提问。
- 用户只问一个非常具体的小问题时，可建议快速模式；不能借此跳过必要的对象消歧。
- 一张卡优先让用户勾选，不要求用户填写冗长研究设计。
- “输出视角/内容形式”是**可选项**：用户未表达偏好且研究目的已足以决定呈现重点时，默认采用“中性通用”视角，不额外打断；研究目的无法推断呈现需求、用户明确提出不同受众、或结果将用于重要决策/对外材料时，再将此项加入选择卡。

### 中性默认视角与内容形式

本 Skill 不预设用户职业、行业、技术能力、决策权限或知识背景。默认以**中性、清晰、可回查**的方式解释研究发现；避免假定用户是产品经理、开发者、采购者或任何其他角色。

| 视角/内容形式 | 适用偏好 | 默认呈现重点 |
|---|---|---|
| 中性通用（默认） | 未说明受众或角色 | 事实、证据、争议、限制与可选下一步 |
| 决策行动 | 希望快速决定买/用/选/去不去 | 适用条件、关键风险、待确认事项、行动清单 |
| 产品/业务洞察 | 研究市场、竞品或用户需求 | 用户动机、痛点、替代方案、机会假设 |
| 风险避坑 | 优先排查负面与失败案例 | 风险项、触发条件、验证方法、规避动作 |
| 证据优先 | 希望自行判断或复用材料 | 证据卡、原话摘要、链接、样本边界，少做推断 |
| 用户自定义 | 用户指定读者、用途或文风 | 按用户要求调整，但不得降低来源与限制声明要求 |

### 输出阶段与正式交付（硬规则）

**必须区分“阶段性结论”和“最终报告”，不得用聊天中的阶段性内容冒充正式报告。**

1. **阶段性结论**：用于对象消歧、首轮试搜、定向补搜或用户要求即时查看时。开头必须用独立醒目的一行标注：`## 阶段性结论（尚非最终报告）`；同时说明已覆盖范围、尚未覆盖的证据缺口、下一步可选路径。阶段性结论不要求落盘，也不得使用“最终结论”“正式报告已完成”等表述。
2. **最终报告**：仅当用户明确选择“直接输出报告”、明确要求“最终报告/正式报告”，或在定向调研收束后确认报告形式时生成。生成前，主 Agent 必须用 `mcp__ask-user__ask_user` 询问或确认用户期望的报告形式；用户已明确指定时只复述确认，不重复提问。
3. **无明确偏好时的推荐**：主 Agent 可按研究目的推荐 1 个报告形式，但仍必须提供“采用推荐形式”的选项；不得自行把推荐当作用户选择。
4. **正式交付要求**：最终报告必须按“报告模板（落盘格式）”写入当前工作区 `{主题}-社区洞察调研报告.md`，验证文件存在且可读后，才能在聊天中称为“最终报告”或“正式报告”。聊天正文只提供简短摘要与文件路径/链接。
5. **用户明确不要文件时**：可在聊天中输出完整最终报告，但开头必须标注 `# 最终报告（未落盘，按用户要求）`，并保留模板中的证据、限制与来源结构；不得省略来源链接。

### 其余可选参数

| 参数 | 默认值 | 说明 |
|---|---|---|
| 平台 | 小红书 + 知乎 | 单选或多选；选 1 个→单平台报告，选 ≥2 个→自动做交叉对照 |
| 样本量 | 按深度动态设定 | 快速：每个有效平台 8–15 条候选、精读 3–5 条；常规：每平台 15–30 条候选、精读 5–8 条；深度：每平台 ≥30 条候选、精读 8–12 条。样本不足须说明，不能凑数。 |
| 是否读评论 | 是（仅代表帖） | 默认只读代表帖、争议帖和高互动帖的评论；用户明确要求时再扩大。 |
| 是否识别软广 | 是 | 输出单独“软广识别清单”章节。 |
| 是否官方核实 | 按需 | 只有报告需要对价格、套餐、功能、版本、政策等硬事实下结论时，才核验官方来源。 |
| 报告模式 | 用户确认；无偏好时先推荐 | 决策版 / 产品洞察版 / 避坑版 / 市场舆情版 / 证据库版。 |
| 输出视角/内容形式 | 中性通用；可选 | 见“中性默认视角与内容形式”章节。 |
| 报告落盘路径 | 当前工作区 | 文件名规则：`{主题}-社区洞察调研报告.md`；除非用户明确不要文件，否则最终报告必须落盘。 |

---

## 子 Agent 的启用规则

### 默认策略

**默认不启用子 Agent。** 主 Agent 负责澄清研究问题、控制方向、汇总冲突证据并与用户沟通。子 Agent 只处理可独立验证的搜集或核验工作，不能替代用户的业务判断，也不能独自作最终结论。

### 何时启用

同时满足以下条件时才启用：

1. 用户选择“常规调研”或“深度调研”；
2. 工作能拆成至少两个互不依赖的单元；
3. 拆分能明显提升覆盖度或速度，而不是制造重复；
4. 主 Agent 已锁定研究问题、对象和优先维度。

可拆分的方式：按平台、目标人群、竞品、或“社区口碑搜集 / 官方硬事实核验”拆分。每个子 Agent 必须收到相同的研究简报、明确样本边界和链接保留规则。

### 何时禁止启用

- 快速摸底；
- 用户只问 2–3 个具体问题；
- 对象或调研目标尚未澄清；
- 工作只是重复搜索同一平台、同一关键词；
- 需要用户在中间做价值取舍时。

> 子 Agent 输出是候选证据，不是最终事实或最终建议；主 Agent 必须去重、标注来源、呈现冲突，并把方向选择权交回用户。

---

## 平台选择、风控与登录态处理

### 平台选择：AI 推荐，用户最终确认

**硬规则：AI 有推荐权，用户有最终选择权。** 平台默认值或 AI 推荐均不得覆盖用户明确的平台偏好、排除要求或目标用户判断。

1. 主 Agent 根据研究简报的调研目的、目标人群、产品类型和地域范围，提出 **1–3 个优先平台及理由**，并说明未优先选择的平台可能遗漏什么视角。
2. 用 `mcp__ask-user__ask_user` 提供简短选择：**采用推荐 / 删减平台 / 仅选一个平台 / 自己指定平台**。用户已明确平台时，只复述并记录，不重复询问。
3. 用户未表达偏好时，可采用与主题匹配的推荐默认值；报告和研究简报中必须写明“平台由 AI 推荐后采用”，不能表述为用户指定。
4. 平台不是越多越好：快速模式建议 1–2 个；常规调研建议 2–3 个；深度调研才按证据缺口扩展到 3 个以上。新增平台必须能补足不同人群、地区、内容形态或观点，而非重复搜同一类声音。

**示例推荐逻辑（方法建议，不是固定事实结论）**：

| 研究情境 | 可优先推荐 | 主要补充价值 | 典型局限 |
|---|---|---|---|
| 中文消费/订阅决策 | 小红书 + 知乎 | 消费体验 + 长篇比较 | 软广与自选择样本偏差 |
| 开发者工具/AI 模型 | 知乎 + V2EX + Reddit/X（可用时） | 技术细节、实践经验、海外声音 | 尝鲜用户不等于生产用户 |
| 视频/硬件/游戏 | B站 + 小红书或知乎 | 测评演示 + 日常体验 | 商业测评和标题党风险 |
| 海外 SaaS/AI 产品 | Reddit + X + YouTube（可用时） | 海外用户、讨论时效性 | 与中文用户需求可能不同 |

### 调研前连通性与登录态检查

在首轮试搜前，对用户最终确认的平台逐一做**一次轻量检查**，确认：搜索是否可用、是否要求登录、当前登录态是否有效，以及正文/评论能力是否受限。检查只为判断可用性，不计入样本量。

- 平台正常：记录可用能力后继续试搜。
- 单项能力受限（如评论不可读）：继续允许的公开内容，并在研究简报和最终限制声明中写清楚“缺失什么、会影响什么”。
- 登录态失效：按“登录态失效”流程暂停该平台，不把报错或空结果当作无相关内容。
- 明确访问受限/风控：按“访问受限或风控”流程停止该平台的自动请求。

### 访问受限或风控：停止、记录、由用户决定

出现明确验证码、访问过于频繁、账号异常、限流提示、连续异常空结果、反爬提示或封禁提示时，视为**平台访问受限**。它不同于单条内容超时。

**硬规则**：

1. **立即停止**该平台后续批量搜索、重试和变体搜索；不能试图绕过验证码、访问限制、反爬或其他平台安全措施。
2. 保留在受限前已经合法取得的公开样本，记录时间、已完成内容、未完成内容和原始异常提示（不泄露凭据）。
3. 先判断该平台对本轮研究是否关键，再用 `mcp__ask-user__ask_user` 说明实际影响并提供选择：
   - 仅用其余平台完成，并在限制声明中说明；
   - 选定一个**非等价替代平台**补充特定视角；
   - 保留已取得样本，但降低该平台证据权重；
   - 暂停本轮，等待用户稍后决定。
4. 替代平台必须说明“补的是什么视角、不能替代什么视角”。不得因找到了替代平台，就声称原平台已完成覆盖。
5. 报告必须写入：原计划平台、实际覆盖情况、受限原因、替代方案、对结论的潜在偏差。

**错误分级**：

| 情况 | 处理 | 最大重试 |
|---|---|---:|
| 单条正文/评论超时 | 记录后重试一次，仍失败则跳过 | 1 次 |
| 单项功能暂时不可用 | 该功能停止，保留其他公开能力 | 0–1 次 |
| 平台访问受限/风控 | 停止该平台自动请求，交由用户决策 | 0 次 |

### 登录态失效：不索取凭据，由用户通过正常界面恢复

**硬规则：绝不在对话中索取、接收或记录账号、密码、验证码、Cookie、Token 等凭据。** 不自动绕过登录、验证码或人机验证。

检测到登录失效后：

1. 说明**哪个平台**登录失效，以及会影响哪些能力（例如搜索、正文、评论或仅限部分内容）。
2. 用 `mcp__ask-user__ask_user` 提供选择：
   - 用户在产品提供的正常登录/授权界面完成重新登录，然后重新检查并继续；
   - 本轮跳过该平台，选替代平台；
   - 仅使用无需登录的公开内容，并降低该平台结论权重；
   - 暂停调研。
3. 用户选择重新登录后，等待用户明确表示已完成；再执行一次轻量检查。检查成功才恢复抓取，检查失败则再次交给用户选择，不能假装恢复成功。
4. 最终报告必须说明登录限制造成的内容缺口及其对目标人群覆盖的影响。

### 面向用户的异常提示格式

发生风控或登录失效时，先用非技术语言说明：**发生了什么 → 已停止什么操作 → 已保留什么 → 会影响什么 → 可选路径**。不要只抛出错误码，也不要默认静默换平台。

---

## 平台支持矩阵（历史兼容性参考）

> 本表仅记录维护者在特定日期和环境中的历史测试观察，不构成兼容性、持续可用性、平台授权或服务质量承诺。实际能力会受 OpenCLI 版本、浏览器、登录态、地区、账户权限和平台策略影响；每次运行前必须轻量探测并遵守目标平台规则。
> 标签含义：**历史验证** = 曾在维护者环境中测试；**待探测** = 适配器/命令存在但未完成真实抓取验证；**不适用** = 未发现适合本 Skill 的内容搜索能力。

| 平台 | opencli 站点名 | 搜索 | 评论 | 正文/详情 | 登录要求 | 状态 |
|---|---|---|---|---|---|---|
| **小红书** | `xiaohongshu` | 历史验证 | 历史验证 | ⚠️ 正文可能超时 | 需用户自行登录；运行时检查 | 历史验证 |
| **知乎** | `zhihu` | 历史验证 | 历史验证 | 历史验证 | 需用户自行登录；运行时检查 | 历史验证 |
| 小红书海外版 | `rednote` | 历史验证 | 历史验证 | 历史验证 | 可能需登录；运行时检查 | 待探测 |
| 微博 | `weibo` | ⚠️ 需登录 | ✅ | ✅ post | **需登录，未登录搜索返回 NOT_FOUND** | ⚠️依赖登录态 |
| B站 | `bilibili` | 历史验证 | 历史验证 | 历史验证 video/summary | 平台规则与实际状态为准 | 历史验证 |
| 抖音 | `douyin` | ✅ | ⚠️ 仅 user-videos 附评论 | ✅ videos | 可能需登录 | 🔍待探测 |
| 豆瓣 | `douban` | ✅ | ❌ 无评论命令 | ✅ subject | 大部分公开 | 🔍待探测 |
| 百度贴吧 | `tieba` | ✅ | ✅ read | ✅ read | 大部分公开 | 🔍待探测 |
| X/Twitter | `twitter` | ✅ | ✅ thread | ✅ article | 需登录 | 🔍待探测 |
| YouTube | `youtube` | ✅ | ✅ | ✅ video/transcript | 大部分公开 | 🔍待探测 |
| Reddit | `reddit` | ✅ | ✅ read(含评论) | ✅ read | 大部分公开 | 🔍待探测 |
| 即刻 | `jike` | ✅ | ✅ post(含评论) | ✅ post | 可能需登录 | 🔍待探测 |
| 雪球 | `xueqiu` | ✅ | ✅ | ✅ stock | 大部分公开 | 🔍待探测 |
| 虎扑 | `hupu` | ✅ | ⚠️ 无独立评论命令 | ✅ detail | 大部分公开 | 🔍待探测 |
| V2EX | `v2ex` | ❌ 无搜索 | ✅ replies | ✅ topic | 公开 | 🔍待探测 |
| 微信公众号 | `weixin` | ✅ 搜狗微信 | ❌ | ✅ download | 公开 | 🔍待探测 |
| 今日头条 | `toutiao` | ❌ 无搜索 | ❌ | ❌ | 公开 | ❌不适用调研 |
| 脉脉 | `maimai` | ❌ 仅候选人 | ❌ | ❌ | — | ❌不适用调研 |
| 视频号 | `wechat-channels` | ❌ 仅发布 | ❌ | ❌ | — | ❌不适用调研 |

**平台选择建议**：
- 中文消费决策（套餐/订阅/数码/生活方式）→ 小红书 + 知乎（信息密度最高）
- 技术/开发者 → 知乎 + V2EX + Reddit + X
- 财经/股票 → 雪球 + 微博 + 知乎
- 视频向测评 → B站 + YouTube + 抖音
- 海外用户口碑 → Reddit + X + YouTube

---

## 执行流程：先共创、后证据、再收束

### 阶段 A · 建立研究简报（搜索前的用户判断点）

在对象消歧前后，主 Agent 根据用户已经提供的信息，整理并展示一段不超过 6 行的**研究简报**：

```text
调研对象：{锁定对象或待确认对象}
本次目的：{购买/选型｜产品/竞品洞察｜避坑/风险｜市场舆情｜内容选题}
优先问题：{用户最在意的 1–3 个维度}
目标人群：{人群}
调研深度与协作方式：{快速/常规/深度}｜{快速/协作/研究模式}
输出视角/内容形式：{中性通用/决策行动/产品业务洞察/风险避坑/证据优先/用户自定义}
初始平台与不纳入范围：{平台及理由}
```

信息缺失时，用 `mcp__ask-user__ask_user` 一次性询问；可提供“采用推荐默认值”的选项。收到选择后**立即开始下一步**，不得只复述选择或再问同一问题。

**三个协作模式**：

| 模式 | 默认节奏 | 适合情况 |
|---|---|---|
| 快速模式 | 确认研究简报 → 试搜/收束 → 简短结论 | 用户只需快速摸底或回答具体问题 |
| 协作模式（默认） | 确认研究简报 → 首轮试搜 → 用户选方向 → 定向调研 → 报告 | 大多数购买、选型和竞品研究 |
| 研究模式 | 确认研究简报 → 首轮试搜 → 每个重大分歧、范围变化或结论框架都交用户确认 | 重要采购、产品战略、对外材料 |

### 阶段 B · 对象消歧（只在确有歧义时打断）

### Step 0 · 对象消歧（先确认调研对象，再动手）

**目的**：防止"同名产品答错对象"。一个主题词下可能挂着多个完全不同的东西（如 "cola" = 可口可乐 / COLA 架构 / 漫画 App / 宠物用品 / AI 工具 ColaOS）。

1. 用 1 个最宽泛的关键词（直接是主题词本身）跑一次搜索，扫一眼结果里是否出现**多个明显不同的对象**。
2. 存在歧义时，用 ask_user 让用户确认调研哪个；用户说"就按最常见的处理"则选搜索结果里占比最高的那个。
3. 报告"核心结论"开头**必须声明锁定对象**："本报告调研对象为 XX（官网 xx），已排除同名 XX/XX"。若当时未确认，就写"按最常见对象 XX 处理，若你指的是别的同名产品请告知"。

> 消歧搜索只用于确认方向，不计入样本量。

### Step 1 · 设计“首轮试搜”关键词（少而有目的）

不要默认生成 4 类、每类至少 2 个关键词并全量搜索。先根据研究简报设计 **3–5 个首轮关键词**，覆盖：

| 关键词方向 | 是否必选 | 何时使用 | 示例（主题=OpenCode Go） |
|---|---:|---|---|
| 对象/品牌词 | 是 | 建立基础候选池、检查对象是否锁定 | `OpenCode Go` |
| 与优先问题对应的词 | 是 | 找与用户决策直接相关的证据 | `OpenCode Go 退款`、`OpenCode Go 团队使用` |
| 反向或争议词 | 建议 | 防止只搜到单一正/负面 | `OpenCode Go 缺点` |
| 替代/竞品词 | 仅需要时 | 用户要做选型、机会或替代方案研究 | `AI 编程工具 平替` |
| 人群/场景词 | 仅需要时 | 用户指定目标人群或使用场景 | `团队协作 OpenCode Go` |

**原则**：搜索词服务于研究问题，不为凑数量；跨平台使用核心词以便比较，但允许按平台语言习惯补充变体。

### Step 2 · 推荐、确认并检查平台

1. 依据“平台选择：AI 推荐，用户最终确认”提出平台建议；用户已指定平台时不覆盖其选择。
2. 用 `mcp__ask-user__ask_user` 让用户确认最终平台范围（采用推荐 / 删减 / 单平台 / 自定义）。快速模式若用户已明确“按推荐做”，可不再打断。
3. 对每个确认的平台做一次轻量连通性与登录态检查，例如：
   ```
   opencli <site> search "测试" -f json
   ```
   并同时判断搜索、正文和评论能力是否实际可用。
4. 按检查结果处理：
   - 正常 → 加入首轮试搜；
   - 单项能力缺失 → 记录降级和影响后继续可用能力；
   - 要求登录或登录态失效 → 遵循“登录态失效：不索取凭据，由用户通过正常界面恢复”；
   - 明确受限、验证码、风控、封禁或连续异常 → 遵循“访问受限或风控：停止、记录、由用户决定”。
5. 记录每个平台最终实际可用状态、登录限制和降级范围，写进研究简报、数据样本说明和限制声明。不得把“未登录/风控导致的空结果”解释成“没有相关讨论”。

### Step 3 · 首轮试搜：先找信号，不跑满配额

**执行提示：某些 OpenCLI 适配器在特定宿主环境中可能出现数组返回兼容性问题。遇到该问题时，可使用 OpenCLI 命令行并记录运行环境；不要把这一历史观察理解为所有版本或所有宿主的唯一处理方式。**

按研究简报分配首轮工作。单一主 Agent 可按平台依次完成；符合“子 Agent 的启用规则”时，可按平台或独立核验任务并行，但每个子 Agent 都只提交候选证据卡。

每个有效平台执行：
1. **搜索候选池**：运行首轮的 3–5 个关键词，合并去重。快速模式不追求固定数量；常规/深度模式按动态样本目标收集。记录标题、作者、点赞、发布时间、**链接/ID（必须保留到底）**、初步立场及与研究问题的关系。
2. **按信息价值筛选代表帖**：优先选能说明新观点、关键争议、目标人群经验、具体使用场景或可验证事实的内容；不因高赞而重复保留同一种观点。
3. **精读正文和关键评论**：只读代表帖、高互动帖、争议帖、疑似软广帖的正文与评论。评论无法读取时记录降级，**不以未读评论冒充评论结论**。
4. **即时建立证据卡**：每条入选内容同步记录“观点/原话要点、支持或反驳哪个问题、来源类型、软广信号、URL、局限”，供汇总直接使用，避免报告阶段回找。
5. **来源分散自检**：作者、内容形态或立场过度集中时，才补充相应搜索词，不机械扩量。
6. **来源结构统计**：按 **官方号 / 媒体号 / 专业博主 / 普通用户** 计数。若普通用户独立评价占比 <30%，后续结论必须醒目标注“⚠️ 独立样本不足：结论受官方/媒体内容影响大，可信度打折”。
7. **链接保留纪律（硬规则）**：所有被纳入报告的帖子、回答、官方页面和评论，URL 必须随样本进入最终 Markdown；可放在表格“链接”列或 `[标题](URL)` 中。禁止只用“知乎回答 #3”等代号。
8. **定量引用纪律**：点赞数 ≥10 的评论/帖子才能用于定量对比；低于 10 赞仅作定性参考。跨平台比较同一观点时，同时标注两端互动量和样本局限。

各平台命令速查见下方“平台命令速查表”。

### 阶段 C · 首轮发现确认（协作模式的核心判断点）

首轮试搜结束后，**协作模式与研究模式必须暂停扩搜**，用 `mcp__ask-user__ask_user` 向用户展示简洁的“调研雷达卡”。快速模式只在发现重大歧义、证据冲突、重大风险或证据严重不足时展示此卡。

如在聊天正文中展示任何首轮或补搜发现，必须先加标题：`## 阶段性结论（尚非最终报告）`。不得使用“最终结论”“完整报告”或容易使用户误解为已正式交付的措辞。

调研雷达卡必须包括：

```text
已覆盖：{平台、目标人群、代表样本数}
初步优点/支持证据：{不超过 3 点}
初步问题/反对证据：{不超过 3 点}
主要争议或平台差异：{没有也须说明}
仍缺的关键证据：{如：近期版本、企业用户、付费价值、负面问题是否已修复}
```

提供可选下一步：
1. 深挖负面与避坑；
2. 深挖价格/付费价值或官方硬事实；
3. 比较竞品与替代方案；
4. 聚焦某一类目标人群/场景；
5. 补齐指定平台或近期样本；
6. 当前证据已够，直接输出报告。

用户可多选或自定义方向。收到选择后，立即执行定向补搜；**不得重新启动无差别全量搜索**。若用户选择直接出报告，跳至 Step 7。

### 阶段 D · 定向补搜与收束

根据用户选定方向，仅补能弥补证据缺口的关键词、平台、目标人群或官方来源。每轮补搜后检查以下停止条件：

- 正反观点、主要争议及其适用条件已有足够代表证据；
- 新增样本主要重复已有观点；
- 用户指定的问题已被回答，或明确要求停止；
- 平台/人群仍有缺口但无法获得可靠样本，且已记录局限。

达到停止条件即进入交叉验证与报告，不为满足固定帖数继续抓取。研究模式在“扩大平台/人群范围”“改变结论框架”“发现重大且未核实的风险”前额外请求用户确认。

### Step 4 · 探测与降级（关键：宁可明示缺失，不可假装成功）

| 故障 | 降级策略 | 报告里怎么写 |
|---|---|---|
| 小红书正文 `note` 超时（60s） | 降级为"搜索标题 + 评论区"，不再重试正文 | "正文未读取，讨论观点基于标题+评论归纳" |
| 某平台评论接口失败 | 用该平台搜索结果 + 其他平台评论补齐 | 标注该平台"评论缺失" |
| 登录态失效 | 停止该平台；由用户通过正常登录界面恢复，或选择替代/降级 | 标注“登录限制及受影响能力” |
| 明确风控/访问受限 | 立即停止该平台自动请求，保留已有公开样本，交用户决定替代/缩小/暂停 | 标注“受限原因、未完成范围及结论偏差” |
| 某平台整体不可用（非风控） | 明确告知，交用户选择换平台或缩减范围 | 记录“该平台被排除及原因” |
| 单条抓取超时 | 重试 1 次，仍失败跳过，不阻塞 | 标注“N 条未抓到” |

**硬规则**：任何降级都必须在报告的“限制声明”里写明，**绝不允许**用“系统提示缺失”冒充成功。风控和登录失效时不得静默换平台、不得绕过限制、不得索取任何凭据。

### Step 5 · N 平台交叉验证

- 选 1 个平台 → 单平台报告（讨论点矩阵）。
- 选 ≥2 个平台 → 输出**三栏对照表**：
  - **共识**：多平台一致出现的观点（可信度高，重点写）
  - **争议**：平台间或平台内分裂的观点（并列给出正反证据）
  - **平台独有**：只有某个平台才有的视角（如知乎的技术深度、小红书的情绪化表达）

### Step 6 · 按需官方事实核实

只要报告将对 **价格、套餐、功能、上线时间、是否免费、版本变更、政策或合规状态** 等硬事实下结论，必须查官方来源；纯主观体验类调研可跳过。用户在“调研雷达卡”选择“深挖价格/官方事实”时优先执行此步骤。

1. 抓官方来源：官网、官方公告、官方定价页（`opencli weixin download` 抓官方公众号文章，或 WebFetch 官网链接）。
2. 把核实结果写进报告“官方事实核实”小节，标注：**✅ 已核实**（有对应官方来源）或 **⚠️ 未核实**（如实说明社区数据可能滞后/失真）。
3. 硬规则：**宁可写“未核实”，不可用社区口碑冒充官方事实。**

### 阶段 E · 确认报告形式并生成最终报告

完成定向调研、且满足收束条件后，先在聊天中用 `## 阶段性结论（尚非最终报告）` 简要说明收束原因、覆盖范围和仍存限制；**不得直接把该内容当作最终报告交付。**

随后必须用 `mcp__ask-user__ask_user` 确认最终报告形式：用户已明确指定时，只复述确认并进入生成；用户未指定时，可推荐 1 个形式，但必须提供“采用推荐形式”以及其他可选项，不能自行替用户决定。快速模式同样适用此规则，除非用户在一开始已明确指定报告形式或明确说“不要正式报告/只要聊天结论”。研究模式必须确认。

| 报告形式 | 适合目的 | 必须突出 |
|---|---|---|
| 决策版 | 购买/选型 | 是否建议选、适用条件、关键风险、下一步验证 |
| 产品洞察版 | 产品/竞品研究 | 用户动机、痛点、现有替代、机会假设 |
| 避坑版 | 风险核查 | 风险项、触发条件、受影响人群、规避或验证动作 |
| 市场舆情版 | 了解公众观点 | 正反观点、争议、平台差异、近期变化 |
| 证据库版 | 用户自己继续分析 | 可回查原始链接、证据卡与分类标签，少做推断 |

### Step 7 · 生成最终报告并落盘

除非用户明确要求不要文件，按下方模板及用户**确认**的报告形式生成 Markdown，写入工作区 `{主题}-社区洞察调研报告.md`。落盘后必须用 Read/Bash 验证文件存在且可读，再交付。只有完成此流程，才能使用“最终报告”“正式报告”或“报告已完成”等表述。报告开头必须记录：研究目的、优先问题、目标人群、协作模式、输出视角/内容形式、用户在首轮后作出的方向选择（如有）、用户确认的报告形式。

若用户明确要求不落盘，则在聊天中按完整模板输出，并以 `# 最终报告（未落盘，按用户要求）` 开头；同样必须保留来源链接、限制声明和信息源标注。

**链接落盘格式（硬规则）**：
- 所有被引用的样本（帖子 / 回答 / 官方页面 / 评论），必须在报告里以**可点击 URL** 形式出现：表格中加"链接"列，或文中用 `[标题](URL)`。
- 模板的三大引用表（讨论点矩阵 / 软广识别清单 / 官方事实核实）默认都含"链接"列；共识/争议/黑天鹅、跨平台交叉验证每条证据都必须带 URL。
- 文末附录"完整来源索引"把所有 URL 汇总，方便用户一键回查。
- URL 缺失或失效（如小红书短链失效、知乎匿名无法直达）必须在该条脚注里写"⚠️ 链接失效 / 已删除 / 需登录"，不可静默省略。

---

## 平台命令速查表（opencli · Bash 直调）

通用格式：`opencli <site> <command> [参数] -f json`（`-f json` 便于解析；`-f md` 适合直接阅读）。

### 小红书（xiaohongshu）
```bash
opencli xiaohongshu search "<关键词>" -f json          # 搜索笔记
opencli xiaohongshu note "<note-id>" -f json           # 笔记正文+互动数据（⚠️可能超时）
opencli xiaohongshu comments "<note-id>" -f json       # 笔记评论（含楼中楼）
opencli xiaohongshu user "<user-id>" -f json           # 某用户公开笔记
opencli xiaohongshu whoami                             # 查看登录账号
```

### 知乎（zhihu）
```bash
opencli zhihu search "<关键词>" -f json                # 搜索（all/answer/question/article 可加 --type）
opencli zhihu question "<question-id>" -f json         # 问题详情+全部回答
opencli zhihu answer-detail "<answer-id>" -f json      # 单个回答完整内容
opencli zhihu answer-comments "<answer-id>" -f json    # 回答评论
opencli zhihu hot -f json                              # 热榜
opencli zhihu whoami                                   # 查看登录账号
opencli zhihu download --url "<专栏链接>" -f json      # 专栏文章正文（zhuanlan.zhihu.com）
```

### 微博（weibo）
```bash
opencli weibo search "<关键词>" -f json                # 搜索
opencli weibo post "<id>" -f json                      # 单条微博
opencli weibo comments "<id>" -f json                  # 评论
opencli weibo hot -f json                              # 热搜
opencli weibo user-posts "<user-id>" -f json           # 某用户微博列表
```

### B站（bilibili）
```bash
opencli bilibili search "<关键词>" -f json             # 搜索视频/用户
opencli bilibili video "<bvid>" -f json                # 视频元数据
opencli bilibili comments "<bvid>" -f json             # 视频评论（官方API，--parent 读楼中楼）
opencli bilibili summary "<bvid>" -f json              # 官方AI总结
opencli bilibili ranking -f json                       # 排行榜
```

### 抖音（douyin）
```bash
opencli douyin search "<关键词>" -f json               # 搜索视频
opencli douyin user-videos "<sec_uid>" -f json         # 用户视频（含热门评论）
opencli douyin videos -f json                          # 作品列表
```

### 豆瓣（douban）
```bash
opencli douban search "<关键词>" -f json               # 搜索电影/图书/音乐
opencli douban subject "<id>" -f json                  # 条目详情
opencli douban movie-hot -f json                       # 电影热榜
opencli douban book-hot -f json                        # 图书热榜
```
> 注：豆瓣无独立评论命令，口碑调研优先级低。

### 百度贴吧（tieba）
```bash
opencli tieba search "<关键词>" -f json                # 搜索帖子
opencli tieba read "<id>" -f json                      # 帖子正文+回帖
opencli tieba posts "<forum>" -f json                  # 某吧帖子列表
opencli tieba hot -f json                              # 热门
```

### X / Twitter（twitter）
```bash
opencli twitter search "<关键词>" -f json              # 搜索推文（--from/--has 过滤）
opencli twitter thread "<tweet-id>" -f json            # 帖+回复串
opencli twitter tweets "<username>" -f json            # 某用户推文
opencli twitter article "<tweet-id>" -f json           # 长文导出 Markdown
opencli twitter trending -f json                       # 趋势
```

### YouTube（youtube）
```bash
opencli youtube search "<关键词>" -f json              # 搜索视频
opencli youtube video "<url>" -f json                  # 视频元数据
opencli youtube comments "<url>" -f json               # 视频评论
opencli youtube transcript "<url>" -f json             # 字幕/文字稿
```

### Reddit（reddit）
```bash
opencli reddit search "<关键词>" -f json               # 搜索帖子
opencli reddit read "<post-id>" -f json                # 帖子+全部评论（口碑调研核心）
opencli reddit subreddit "<name>" -f json              # 某 subreddit 帖子
opencli reddit user-posts "<username>" -f json         # 某用户发帖
```

### 即刻（jike）
```bash
opencli jike search "<关键词>" -f json                 # 搜索
opencli jike post "<id>" -f json                       # 帖子详情+评论
opencli jike topic "<id>" -f json                      # 话题/圈子
```

### 雪球（xueqiu）
```bash
opencli xueqiu search "<关键词>" -f json               # 搜索股票
opencli xueqiu comments "<symbol>" -f json             # 某股票的讨论动态
opencli xueqiu hot -f json                             # 热门动态
```

### 虎扑（hupu）
```bash
opencli hupu search "<关键词>" -f json                 # 搜索
opencli hupu detail "<tid>" -f json                    # 帖子详情
opencli hupu hot -f json                               # 热门
```

### V2EX（v2ex）
```bash
opencli v2ex hot -f json                               # 热门话题
opencli v2ex topic "<id>" -f json                      # 主题+回复
opencli v2ex replies "<id>" -f json                    # 主题回复列表
opencli v2ex node "<name>" -f json                     # 节点话题
```

### 微信公众号（weixin）
```bash
opencli weixin search "<关键词>" -f json               # 搜狗微信搜索文章
opencli weixin download                                # 文章链接导出 Markdown
```

---

## 软广识别清单（4 信号 + 处理）

### 4 个信号（命中即标"疑似软广"）

| # | 信号 | 判定强度 |
|---|---|---|
| 1 | **带邀请/返利链接**：内容里含 `?ref=...`、`?invite=...`、专属优惠码 | 🔴 100% affiliate 软广 |
| 2 | **同作者多篇推荐**：同一 ID 在多个关键词下反复推同一产品 | 🟠 商业合作概率极高 |
| 3 | **营销话术**："官方合作伙伴"、"限时 X 折"、"程序员别再原价冲"、引导"点击我的链接" | 🟠 强商业导向 |
| 4 | **无数据纯夸**：只有"性价比高/好用/闭眼入"等模糊话术，无价格、无场景、无数值 | 🟡 疑似软广 |

### 附加核查
- 查看作者主页：粉丝量是否异常低却高赞？昵称是否带品牌词？
- 官方账号、转载官方内容、标注"转载"的账号 → 属官方宣传，不算普通用户口碑。
- 高赞的"订阅介绍/教程"类内容往往藏着 affiliate 链接，**引用数据可以，引用"推荐结论"要打折**。

### 报告中的输出格式
每个被识别为软广的来源，单独列一行：
```
| 作者 | 平台 | 点赞 | 信号命中 | 判定 | 证据 | 链接 |
```

---

## 报告模板（落盘格式）

```markdown
# {主题} 社区洞察调研报告

> 抓取时间：{日期} ｜ 平台：{平台列表} ｜ 数据样本：{N} 条帖子/回答 + {M} 条评论 ｜ 调研对象：{消歧后锁定的对象}
> 研究目的：{购买/选型｜产品/竞品洞察｜避坑/风险｜市场舆情｜内容选题} ｜ 优先问题：{1–3 项} ｜ 目标人群：{人群}
> 调研方式：{快速/常规/深度} ｜ {快速/协作/研究模式} ｜ 输出视角/内容形式：{中性通用/决策行动/产品业务洞察/风险避坑/证据优先/用户自定义}
> 首轮后用户方向选择：{选择内容；未发生则写“不适用”} ｜ 报告形式确认：{用户选择/采用推荐/不适用}
> 报告形式：{决策版/产品洞察版/避坑版/市场舆情版/证据库版}
> 📎 完整样本 URL 见 `## 三、讨论点矩阵`、`## 六、软广识别清单`、`## 七、官方事实核实` 链接列；统一索引见文末 `## 附录A·完整来源索引`。

## 一、核心结论
一句话定性 + 一句话关键提醒（如"口碑两极化"、"存在信任危机"）。
> ⚠️ 若普通用户独立评价占比 <30%，此处必须标红"独立样本不足，结论可信度打折"。

## 二、数据样本说明
表格：平台 / 关键词 / 命中数 / 代表帖数 / 评论数 / 可用状态（是否降级）。
来源结构占比表：官方号 / 媒体号 / 专业博主 / 普通用户 的占比。

## 三、讨论点矩阵（按热度/共识排序）
| 讨论点 | 代表内容 | 平台/作者 | 点赞 | 链接 | 立场 |
（Top 5 讨论主线，附代表帖、点赞数、**链接必填**。链接缺失则在备注栏标"⚠️ 链接失效 / 已删除 / 需登录"。）

## 四、共识 vs 争议 vs 黑天鹅
> 每条证据后必须附 URL（格式：`出处：[作者](URL)（平台 · N 赞）`），方便用户回查源数据。

### ✅ 共识（高赞同度）
- 共识 1：…  出处：[作者](URL)（小红书 · 64 赞）
- 共识 2：…  出处：[作者](URL)（知乎 · 35 赞）

### ⚖️ 争议（正反证据并列）
- 争议点：…
  - 正方：[作者A](URL)（平台 · N 赞）"原话或要点"
  - 反方：[作者B](URL)（平台 · M 赞）"原话或要点"

### 🚨 黑天鹅/警示（潜在风险事件）
- 风险 1：…  出处：[作者](URL)（平台 · 日期）

## 五、跨平台交叉验证（仅多平台时输出）
> 每条证据附 URL：`[来源平台](URL)`。

### 共识
### 争议
### 平台独有

## 六、软广识别清单
表格：作者 / 平台 / 信号 / 判定 / 证据 / **链接（必填）**

## 七、官方事实核实
表格：事实项 / 社区口径 / 官方口径 / 核实状态（✅已核实 / ⚠️未核实） / **链接（必填）**。
硬规则：查不到就写"未核实"，不得用社区口碑冒充官方数据。

## 八、决策树（该不该选 / 适合谁）
mermaid flowchart（节点 ≤12，标签 ≤24 汉字）

## 九、限制声明
| # | 限制 | 说明 |
（必须含：样本偏差、独立样本占比、降级情况、未覆盖维度、数据时效、**链接覆盖率**）

## 十、行动建议 / 使用启示
> 按用户确认的输出视角呈现：中性通用视角提供“可选下一步”；决策行动视角提供“行动清单”；产品/业务洞察视角提供“可验证的洞察假设”；风险避坑视角提供“规避与验证动作”；证据优先视角可简化为“待核实问题”。不得假设读者职业。

| 场景/待决问题 | 建议或下一步 |

## 十一、研究过程与用户决策记录
| 节点 | AI 的发现/建议 | 用户的选择 | 对范围的影响 |
|---|---|---|---|
| 搜索前研究简报 | {对象、目的、优先问题} | {用户确认/采用默认} | {初始范围} |
| 首轮调研雷达卡 | {主要信号与证据缺口} | {深挖方向/直接收束} | {定向补搜范围} |
| 报告形式 | {推荐理由} | {用户选择} | {交付结构} |

> 快速模式未发生的节点标“未触发”；不得编造用户选择。该章节用于让用户回看“为什么这样搜、为什么得出这个结论”。

## 十二、信息源标注
[联网核实] 各平台抓取数据（注明工具与时间；具体 URL 见文中链接列或附录 A）
[官方文档] 官方定价/公告页（URL 见 ## 七 链接列）
[凭印象-未核实] 分析结论、行动建议/使用启示、决策树判断

## 附录 A · 完整来源索引（按平台汇总）
> 用途：把全文引用过的 URL 集中放一份，方便用户一键回查原始数据。
> 格式：每个 URL 必须能直接点击；若链接失效，标"⚠️ 已失效"并简述原因（已删除/需登录/短链过期）。

### 小红书（共 X 条）
- [标题](URL) ｜ @作者 ｜ 👍 N 赞
- [标题](URL) ｜ @作者 ｜ 👍 N 赞

### 知乎（共 X 条）
- [标题（问题/回答/专栏）](URL) ｜ @作者 ｜ 👍 N 赞

### 官方/媒体（共 X 条）
- [标题（页面/文章/公告）](URL) ｜ 来源（如：阿里云百炼定价页）

### 其他平台（微博/B站/抖音/Reddit/X/YouTube/V2EX/雪球/贴吧/即刻/豆瓣 等）
- [标题](URL) ｜ @作者/账号 ｜ 👍 N 赞
```

---

## 来源标注规范（对用户硬承诺）

| 标注 | 含义 |
|---|---|
| [联网核实] | 有真实工具返回作为依据（抓到的帖/评论/数据） |
| [官方文档] | 官方来源 |
| [凭印象-未核实] | 模型推断/经验判断，未做联网验证——**所有分析结论、建议、决策树、共识判断必须标注** |

**禁止**：把 [凭印象-未核实] 的内容写成 [联网核实]；把"分析判断"写成"平台原话"。

---

## 验收清单（每轮结束自检）

### 协作与范围
- [ ] 已生成研究简报；缺失信息已用一张简短选择卡确认，或明确记录用户采用默认建议
- [ ] AI 已提出平台推荐及理由；用户已确认、删减或自定义平台。用户明确偏好未被默认值覆盖
- [ ] 每个确认平台已完成一次轻量连通性与登录态检查；实际能力、登录限制和降级范围已记录
- [ ] 如发生风控/访问受限，已停止该平台自动请求、未绕过限制，并由用户选择替代/缩小/暂停路径
- [ ] 如发生登录态失效，未索取或记录任何凭据；仅在用户经正常界面重新登录并检查成功后才恢复抓取
- [ ] 已按用户的调研目的、优先问题、目标人群和深度设计首轮关键词；没有无差别全量搜索
- [ ] 协作模式/研究模式已在首轮后展示调研雷达卡，并记录用户的深挖、转向或收束选择
- [ ] 快速模式若跳过雷达卡，确实没有出现重大歧义、严重冲突、重大风险或证据严重不足
- [ ] 子 Agent（如启用）只承担可独立验证的工作；主 Agent 已去重、汇总并保留最终判断权与用户
- [ ] 所有对象消歧、试搜、补搜或即时发现均以“阶段性结论（尚非最终报告）”明确标识；未将聊天阶段性内容表述为正式交付
- [ ] 生成最终报告前，已由用户明确选择报告形式，或用户明确采用 AI 推荐形式；未自行替用户决定
- [ ] 最终报告已按确认形式与模板落盘、验证可读后才称为“最终报告/正式报告”；用户明确不要文件时，已按完整模板并以“最终报告（未落盘，按用户要求）”标识
- [ ] 输出视角/内容形式采用中性通用默认值，或已按用户选择调整；未预设用户职业、行业、技术能力或决策角色
- [ ] 报告形式与用户目的一致，且报告开头记录了研究过程、用户决策和输出视角

### 证据质量
- [ ] 对象已消歧并锁定（报告开头声明调研对象）
- [ ] 样本量符合所选深度；样本不足或停止原因已说明，不为凑固定数量继续抓取
- [ ] 专业博主 + 普通用户都有覆盖（样本可得时），作者来源和立场已做分散性检查
- [ ] 来源结构占比已统计；普通用户独立样本 <30% 时已标红
- [ ] 已精读代表帖、争议帖和关键评论；未读评论或接口失败已写入限制声明
- [ ] 软广已识别并单独成章
- [ ] ≥2 平台时已做“共识/争议/独有”三栏对照
- [ ] 涉及价格、套餐、版本、功能、政策等硬事实时，已做官方核实或标注“未核实”
- [ ] 最终报告含：核心结论、讨论点矩阵、限制声明、来源标注、行动建议/使用启示、研究过程与用户决策记录
- [ ] 除非用户明确不要文件，最终报告已落盘到工作区，且已用 Read/Bash 验证存在
- [ ] 全程中文、表达清晰、无需假定用户具备特定职业或技术背景
- [ ] 所有被引用的样本（帖子 / 回答 / 官方页面 / 评论）都附了可点击 URL——表格里“链接”列、或文中 `[标题](URL)` 形式，**禁止**仅用代号省略
- [ ] 文末“附录 A · 完整来源索引”已按平台汇总所有 URL；已失效 / 已删除 / 需登录的链接已标注原因
